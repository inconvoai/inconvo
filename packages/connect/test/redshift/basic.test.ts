describe("Redshift dialect support", () => {
  it.todo("handles count aggregations");
  it.todo("supports groupBy JSON aggregations over date intervals");
  it.todo(
    "executes count/countDistinct from a virtual table built from orders + organisations join SQL",
  );
});
